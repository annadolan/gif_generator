<h1>Create a new category:</h1>

<%= form_for [:admin, @category] do |f| %>

<%= f.label :search_term %>
<%= f.text_field :search_term %></p>

<%= f.submit "Create Category", class: "btn btn-success" %>

<% end %>
